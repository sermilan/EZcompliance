---
title: "COU-R-20273-4-4-4-2-a"
cou_id: "COU-R-20273-4-4-4-2-a"
source: "GBT 20273-2006"
chapter: "4-4.4.2"
clause: "a"
subject: "网络安全等级保护对象运营者"
action: "记录"
object: "）、列（字段）、元素（每个元组的字段）、日志、片段、分区、快照、约束和规则、DBMS 核心代码、用户..."
condition: "定级为第4级时"
base_weight: 10.4
weight_factor: 1.3
final_weight: 13.5
domains: ['通用安全']
fingerprint: "网络安全等级保护对象运营者|记录|）、列（字段）、元素（每个元组的字段）、日志、片段、分区、快照、约束和规则、DBMS 核心代码、用户..."
level: "4"
---

# COU-R-20273-4-4-4-2-a

> **来源**: [[GBT 20273-2006]]
> **章节**: 第4级 - 访问控制粒度及特点 - 4.4.2(a)
> **层级**: R (原始COU · Raw)
> **基础权重**: 10.4

## 解剖结构

| 要素 | 内容 |
|------|------|
| 主体 | 网络安全等级保护对象运营者 |
| 动作 | 记录 |
| 客体 | ）、列（字段）、元素（每个元组的字段）、日志、片段、分区、快照、约束和规则、DBMS 核心代码、用户... |
| 条件 | 定级为第4级时 |
| 权重计算 | 10.4 × 1.3 = **13.5** |

## 原文

> 数据以特定结构格式存放，客体的粒度可以是：关系数据库的表、视图、元组（记录）、列（字段）、元素（每个元组的字段）、日志、片段、分区、快照、约束和规则、DBMS 核心代码、用户应用程序、存储过程、触发器、各种访问接口等；

## 元数据

```yaml
cou_id: COU-R-20273-4-4-4-2-a
source: "GBT 20273-2006"
chapter: "4-4.4.2"
clause: "a"
subject: "网络安全等级保护对象运营者"
action: "记录"
object: "）、列（字段）、元素（每个元组的字段）、日志、片段、分区、快照、约束和规则、DBMS 核心代码、用户..."
condition: "定级为第4级时"
base_weight: 10.4
weight_factor: 1.3
final_weight: 13.5
domains: ['通用安全']
fingerprint: "网络安全等级保护对象运营者|记录|）、列（字段）、元素（每个元组的字段）、日志、片段、分区、快照、约束和规则、DBMS 核心代码、用户..."
level: "4"
```
