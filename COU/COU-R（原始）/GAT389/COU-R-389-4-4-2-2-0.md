---
title: "COU-R-389-4-4-2-2-0"
cou_id: "COU-R-389-4-4-2-2-0"
source: "GAT 389-2002"
chapter: "4-4.2.2"
clause: "4.2.2"
subject: "网络安全等级保护对象运营者"
action: "执行"
object: "相应的访问控制程序实现。每当执行 SQL 语句、有访问要求出现时，通过调用相应的访问控制程序，实现对..."
condition: "定级为第4级时"
base_weight: 10.4
weight_factor: 1.3
final_weight: 13.5
domains: ['通用安全']
fingerprint: "网络安全等级保护对象运营者|执行|相应的访问控制程序实现。每当执行 SQL 语句、有访问要求出现时，通过调用相应的访问控制程序，实现对..."
level: "4"
---

# COU-R-389-4-4-2-2-0

> **来源**: [[GAT 389-2002]]
> **章节**: 第4级 - 访问规则 - 4.2.2(4.2.2)
> **层级**: R (原始COU · Raw)
> **基础权重**: 10.4

## 解剖结构

| 要素 | 内容 |
|------|------|
| 主体 | 网络安全等级保护对象运营者 |
| 动作 | 执行 |
| 客体 | 相应的访问控制程序实现。每当执行 SQL 语句、有访问要求出现时，通过调用相应的访问控制程序，实现对... |
| 条件 | 定级为第4级时 |
| 权重计算 | 10.4 × 1.3 = **13.5** |

## 原文

> 应以访问控制表或访问矩阵的形式表示，并通过执行相应的访问控制程序实现。每当执行 SQL 语句、有访问要求出现时，通过调用相应的访问控制程序，实现对访问要求的控制。

## 元数据

```yaml
cou_id: COU-R-389-4-4-2-2-0
source: "GAT 389-2002"
chapter: "4-4.2.2"
clause: "4.2.2"
subject: "网络安全等级保护对象运营者"
action: "执行"
object: "相应的访问控制程序实现。每当执行 SQL 语句、有访问要求出现时，通过调用相应的访问控制程序，实现对..."
condition: "定级为第4级时"
base_weight: 10.4
weight_factor: 1.3
final_weight: 13.5
domains: ['通用安全']
fingerprint: "网络安全等级保护对象运营者|执行|相应的访问控制程序实现。每当执行 SQL 语句、有访问要求出现时，通过调用相应的访问控制程序，实现对..."
level: "4"
```
